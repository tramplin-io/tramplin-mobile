/**
 * Message shown to the user when signing in with wallet.
 * Must match the payload used for createSessionByUserWallet (digest).
 */
export const LOGIN_PAYLOAD = 'Log in to Tramplin App'

/**
 * Message shown to the user when signing in with wallet.
 * Must match the payload used for createSessionByUserWallet (digest).
 */
export const REFERRALS_LOGIN_PAYLOAD = 'Log in to TramplinReferrals'
